<?php

use function Testify\{describe, it, expect};

describe('AnnotationRouter', function () {

    it('should load annotations from controller classes', function () {
        expect(3)->toBe(3);
    });
});
